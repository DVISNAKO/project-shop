import React from 'react';

import styles from '../styles/Footer.module.css';

const Footer = () => {
    return (
        <section className={styles.footer}>
            <div className={styles.content}>
                <h2>Developed by Dima</h2>
                </div>
        </section>
    );
};

export default Footer;